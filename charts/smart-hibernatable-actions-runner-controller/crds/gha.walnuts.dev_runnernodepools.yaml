---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: runnernodepools.gha.walnuts.dev
spec:
  group: gha.walnuts.dev
  names:
    categories:
    - gha
    - all
    kind: RunnerNodePool
    listKind: RunnerNodePoolList
    plural: runnernodepools
    shortNames:
    - rnodepool
    - rnp
    singular: runnernodepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Desired number of physical nodes
      jsonPath: .status.desiredNodes
      name: Desired Nodes
      type: integer
    - description: Powered on nodes count
      jsonPath: .status.poweredOnNodes
      name: Powered On
      type: integer
    - description: Ready nodes count
      jsonPath: .status.readyNodes
      name: Ready Nodes
      type: integer
    - description: Active runners count
      jsonPath: .status.activeRunners
      name: Active Runners
      type: integer
    - description: Pending runners count
      jsonPath: .status.pendingRunners
      name: Pending Runners
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RunnerNodePool is the Schema for the runnernodepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RunnerNodePoolSpec defines the desired state of RunnerNodePool.
            properties:
              clusterRef:
                description: ClusterRef references the RunnerCluster associated with
                  this node pool.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: clusterRef is immutable
                  rule: self == oldSelf
              drain:
                description: Drain defines node draining parameters for machines in
                  the pool.
                properties:
                  timeout:
                    default: 10m
                    description: Timeout is the maximum duration to wait for runner
                      pods to drain before considering the drain stalled.
                    type: string
                type: object
              scaling:
                description: Scaling defines scaling parameters for machines in the
                  pool.
                properties:
                  maxNodes:
                    description: MaxNodes is the maximum number of physical machines
                      allowed to be powered on. If unset, all machines in pool are
                      allowed.
                    format: int32
                    minimum: 1
                    type: integer
                  minNodes:
                    default: 0
                    description: MinNodes is the minimum number of physical machines
                      to keep powered on.
                    format: int32
                    minimum: 0
                    type: integer
                  scaleDownDelay:
                    default: 10m
                    description: ScaleDownDelay is the duration to wait with zero
                      demand before initiating power off.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: minNodes must be less than or equal to maxNodes
                  rule: '!has(self.maxNodes) || self.minNodes <= self.maxNodes'
            required:
            - clusterRef
            type: object
          status:
            description: RunnerNodePoolStatus defines the observed state of RunnerNodePool.
            properties:
              activeRunners:
                description: ActiveRunners is the count of non-terminal runners currently
                  tracked on this pool.
                format: int32
                type: integer
              conditions:
                description: Conditions store the detailed status conditions of the
                  node pool.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredMachines:
                description: DesiredMachines holds the planned desired state for each
                  machine managed by this pool.
                items:
                  description: MachinePlanStatus records the planned desired state
                    for an individual physical machine.
                  properties:
                    desiredState:
                      description: DesiredState is the target state decided by the
                        capacity planner (Active or Off).
                      enum:
                      - Active
                      - "Off"
                      type: string
                    drainStartedAt:
                      description: DrainStartedAt records the timestamp when the machine
                        entered draining for scale-down.
                      format: date-time
                      type: string
                    idleSince:
                      description: IdleSince records the timestamp when this machine
                        became idle (0 active runners and no unschedulable demand).
                      format: date-time
                      type: string
                    name:
                      description: Name is the name of the RunnerMachine resource.
                      type: string
                    uid:
                      description: UID is the unique identifier of the RunnerMachine
                        resource.
                      type: string
                  required:
                  - desiredState
                  - name
                  type: object
                type: array
              desiredNodes:
                description: DesiredNodes is the number of physical machines calculated
                  as needed to satisfy demand.
                format: int32
                type: integer
              idleSince:
                description: IdleSince records the timestamp when runner demand first
                  dropped to zero across all scale sets.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this resource.
                format: int64
                type: integer
              pendingRunners:
                description: PendingRunners is the count of unscheduled or pending
                  runners waiting for compute capacity.
                format: int32
                type: integer
              poweredOnNodes:
                description: PoweredOnNodes is the number of physical machines currently
                  observed as powered on.
                format: int32
                type: integer
              readyNodes:
                description: ReadyNodes is the number of physical machines currently
                  powered on and reporting Kubernetes Node Ready.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
