---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: runnerclusters.gha.walnuts.dev
spec:
  group: gha.walnuts.dev
  names:
    categories:
    - gha
    - all
    kind: RunnerCluster
    listKind: RunnerClusterList
    plural: runnerclusters
    shortNames:
    - rcluster
    - rc
    singular: runnercluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Kubernetes API server reachability
      jsonPath: .status.apiReachable
      name: API Reachable
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RunnerCluster is the Schema for the runnerclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RunnerCluster
            properties:
              kubeconfigSecretRef:
                description: KubeconfigSecretRef references the Secret containing
                  the kubeconfig to connect to the runner Kubernetes cluster.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              readiness:
                description: Readiness defines timeouts and parameters for checking
                  cluster readiness.
                properties:
                  apiRequestTimeout:
                    default: 5s
                    description: APIRequestTimeout is the timeout for Kubernetes API
                      requests to the runner cluster.
                    type: string
                  nodeReadyTimeout:
                    default: 10m
                    description: NodeReadyTimeout is the maximum duration to wait
                      for a physical machine's Kubernetes node to become Ready after
                      power on.
                    type: string
                type: object
              runnerNamespace:
                default: gha-runners
                description: RunnerNamespace is the namespace on the runner cluster
                  where runner Pods and Secrets are created.
                type: string
            required:
            - kubeconfigSecretRef
            type: object
          status:
            description: status defines the observed state of RunnerCluster
            properties:
              apiReachable:
                description: APIReachable indicates whether the runner cluster's Kubernetes
                  API server responded successfully to a health check.
                type: boolean
              clusterUID:
                description: ClusterUID is the unique identifier (kube-system namespace
                  UID) of the remote cluster for split-brain protection.
                type: string
              conditions:
                description: Conditions store the detailed status conditions of the
                  runner cluster.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                default: Unknown
                description: Phase is the current high-level state of the runner cluster.
                enum:
                - Offline
                - Starting
                - Ready
                - Degraded
                - Unknown
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
