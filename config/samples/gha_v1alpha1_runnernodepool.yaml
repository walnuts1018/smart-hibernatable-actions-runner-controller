apiVersion: gha.walnuts.dev/v1alpha1
kind: RunnerNodePool
metadata:
  name: amd64
spec:
  clusterRef:
    name: gha-amd64
  scaling:
    minNodes: 0
    maxNodes: 1
    scaleDownDelay: 10m
  drain:
    timeout: 10m
