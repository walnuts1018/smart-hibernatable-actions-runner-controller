---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ephemeralrunners.gha.walnuts.dev
spec:
  group: gha.walnuts.dev
  names:
    categories:
    - gha
    - all
    kind: EphemeralRunner
    listKind: EphemeralRunnerList
    plural: ephemeralrunners
    shortNames:
    - eprunner
    - er
    singular: ephemeralrunner
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ephemeral runner phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: GitHub Runner ID
      jsonPath: .status.github.runnerID
      name: RunnerID
      type: integer
    - description: Remote Pod name
      jsonPath: .status.remotePod.name
      name: Remote Pod
      type: string
    - description: Remote Node name
      jsonPath: .status.remotePod.nodeName
      name: Node
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EphemeralRunner is the Schema for the ephemeralrunners API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EphemeralRunnerSpec defines the desired state of EphemeralRunner.
            properties:
              runnerName:
                description: RunnerName is the unique name of this runner instance
                  registered in GitHub Actions and used for the remote Pod.
                type: string
              runnerSetRef:
                description: RunnerSetRef optionally references the managing EphemeralRunnerSet.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              scaleSetRef:
                description: ScaleSetRef references the parent RunnerScaleSet.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - runnerName
            - scaleSetRef
            type: object
          status:
            description: EphemeralRunnerStatus defines the observed state of EphemeralRunner.
            properties:
              conditions:
                description: Conditions store the detailed status conditions of the
                  runner.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failure:
                description: Failure contains diagnostic information if the runner
                  failed.
                properties:
                  exitCode:
                    description: ExitCode is the container exit code if available.
                    format: int32
                    type: integer
                  message:
                    description: Message is a human-readable description of the failure.
                    type: string
                  reason:
                    description: Reason is a brief camelCase string indicating the
                      failure reason.
                    type: string
                type: object
              finishedAt:
                description: FinishedAt is the timestamp when the runner reached a
                  terminal state (Completed or Failed).
                format: date-time
                type: string
              gcEligibleAt:
                description: GCEligibleAt is the timestamp after which this EphemeralRunner
                  CR can be garbage collected.
                format: date-time
                type: string
              github:
                description: GitHub holds IDs and job details from GitHub Actions.
                properties:
                  completedObserved:
                    description: CompletedObserved indicates whether a JobCompleted
                      event was received from GitHub Actions.
                    type: boolean
                  completedObservedAt:
                    description: CompletedObservedAt records the timestamp when the
                      JobCompleted event was observed.
                    format: date-time
                    type: string
                  completedResult:
                    description: CompletedResult records the result status string
                      ("completed", "canceled", "failed", etc.) reported by GitHub.
                    type: string
                  jobID:
                    description: JobID is the ID of the job assigned to this runner.
                    format: int64
                    type: integer
                  runnerID:
                    description: RunnerID is the runner ID registered in GitHub Actions.
                    format: int64
                    type: integer
                  runnerRequestID:
                    description: RunnerRequestID is the ID of the runner request in
                      GitHub Actions.
                    format: int64
                    type: integer
                  startedObservedAt:
                    description: StartedObservedAt records the timestamp when the
                      JobStarted event was observed from GitHub Actions.
                    format: date-time
                    type: string
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this resource.
                format: int64
                type: integer
              phase:
                default: Pending
                description: Phase is the current execution phase of the ephemeral
                  runner.
                enum:
                - Pending
                - WaitingForCluster
                - Provisioning
                - Starting
                - Idle
                - Busy
                - Completed
                - Failed
                - Deleting
                type: string
              provisioning:
                description: Provisioning records details of the active or latest
                  JIT provisioning attempt.
                properties:
                  failures:
                    description: Failures is the count of consecutive failed JIT provisioning
                      attempts.
                    format: int32
                    type: integer
                  id:
                    description: ID is the unique attempt identifier (e.g. short hash
                      or sequential counter).
                    type: string
                  jitGeneratedAt:
                    description: JITGeneratedAt is the timestamp when JIT config was
                      generated.
                    format: date-time
                    type: string
                  nextRetryAt:
                    description: NextRetryAt is the timestamp when the next JIT provisioning
                      attempt is allowed.
                    format: date-time
                    type: string
                  runnerID:
                    description: RunnerID is the GitHub Runner ID assigned after JIT
                      generation.
                    format: int64
                    type: integer
                  runnerName:
                    description: RunnerName is the actual runner name registered in
                      GitHub for this attempt.
                    type: string
                  startedAt:
                    description: StartedAt is the timestamp when this provisioning
                      attempt started.
                    format: date-time
                    type: string
                type: object
              remotePod:
                description: RemotePod holds details of the pod running on the remote
                  cluster.
                properties:
                  name:
                    description: Name is the name of the runner Pod on the remote
                      cluster.
                    type: string
                  namespace:
                    description: Namespace is the namespace where the Pod was created
                      on the remote cluster.
                    type: string
                  nodeName:
                    description: NodeName is the name of the remote node hosting the
                      runner Pod.
                    type: string
                  uid:
                    description: UID is the UID of the runner Pod on the remote cluster.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
